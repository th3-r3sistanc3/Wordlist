/_nuxt/@fs
/_nuxt/@fs/etc/passwd
/pages/systemcall.php\?command\=cat%20/etc/passwd
/issues/?filter=
/jira/issues/?filter=
/wp-json/wp/v2/users/
/_rpc?rpc=getConsentScreenDetails
/_rpc?rpc=
/manager/html

During the retest, we observed that MFA is now functioning correctly. However, upon further review of the MFA implementation, we identified that it remains vulnerable to OTP brute-force attacks, which could allow an attacker to bypass the MFA protection. Therefore, the issue remains open.

Additionally, we have added finding 316112:H3 to address the weak MFA implementation identified during the retest.

We also observed that OTPs were not received on the registered email address for the URLs listed below. Could you please register an account using **[securitytesting2@abc.com](mailto:securitytesting2@abc.com)** so that we can verify whether the same issue persists on these URLs as well?


