/_nuxt/@fs
/_nuxt/@fs/etc/passwd
/pages/systemcall.php\?command\=cat%20/etc/passwd
/issues/?filter=
/jira/issues/?filter=
/wp-json/wp/v2/users/
/_rpc?rpc=getConsentScreenDetails
/_rpc?rpc=
/manager/html

As discussed during yesterday's call, could you please provide confirmation regarding the Brazil UI, Discounts UI, and Colombia UI as well?

As mentioned earlier, we are now able to access the Forgot Password and Registration interfaces for these applications. However, based on the information provided, the production environment is configured to use SSO authentication, and these interfaces are not expected to be accessible.
