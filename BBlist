/_nuxt/@fs
/_nuxt/@fs/etc/passwd
/pages/systemcall.php\?command\=cat%20/etc/passwd
/issues/?filter=
/jira/issues/?filter=
/wp-json/wp/v2/users/
/_rpc?rpc=getConsentScreenDetails
/_rpc?rpc=
/manager/html

The server does not provide the complete SSL/TLS certificate chain during the TLS handshake, as the required intermediate certificate is missing. This may cause certificate validation failures for certain clients, applications, or devices that cannot automatically retrieve the missing intermediate certificate.
